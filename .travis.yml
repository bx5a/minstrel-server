language: go

go:
    - 1.6.2
    
addons:
    apt:
        packages:
            - sshpass

script:
    - sed "s/%% paste your developer key %%/$YoutubeAPIDeveloperKey/g" search/conf.go > tmp
    - mv tmp search/conf.go
    - go test -v ./...
    - go build


after_success:
    - sshpass -e scp minstrel-server $MinstrelDeployServerUserName@$MinstrelDeployServerHost:~
    - sshpass -e ssh $MinstrelDeployServerUserName@$MinstrelDeployServerHost ./minstrel-server&